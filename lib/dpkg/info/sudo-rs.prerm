#! /bin/sh
set -e

# Automatically added by dh_installalternatives/13.31ubuntu1
if [ "$1" = "remove" ]; then
	update-alternatives --remove sudo /usr/lib/cargo/bin/sudo
fi
# End automatically added section


case "$1" in
  remove|deconfigure|failed-upgrade)
	rmdir /etc/sudoers.d 2>/dev/null || true
  ;;
esac
