#!/bin/sh
set -e
# Automatically added by dh_installsystemd/13.28ubuntu2
if [ -z "$DPKG_ROOT" ] && [ "$1" = remove ] && [ -d /run/systemd/system ] ; then
	deb-systemd-invoke stop 'grub-initrd-fallback.service' 'grub2-common.service' >/dev/null || true
fi
# End automatically added section
