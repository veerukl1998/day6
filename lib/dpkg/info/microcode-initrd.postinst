#!/bin/sh
set -e
case "$1" in
configure)
    update-microcode-initrd
    if [ -e /boot/grub/grub.cfg ] && which update-grub >/dev/null 2>&1; then
        update-grub || true
    fi
    exit 0
    ;;
triggered)
    update-microcode-initrd
    exit 0
    ;;
esac


